{
  "name": "e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Suite Playwright de humo por rol (coach/coachee/empresa) contra el stack real de docker compose.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
